{
  "name": "marketplace-examples",
  "image": "mcr.microsoft.com/devcontainers/python:3.13",
  "features": { "ghcr.io/devcontainers/features/common-utils:2": {} },
  "postCreateCommand": "bash -lc 'set -euxo pipefail; export PATH=$HOME/.local/bin:$PATH; curl -LsSf https://astral.sh/uv/install.sh | sh; uv --version; make init || uv sync; uv run python -m ipykernel install --user --name inait-uv --display-name \"Python (inait-uv)\"'",
  "customizations": {
    "vscode": {
      "settings": {
        "python.defaultInterpreterPath": "${workspaceFolder}/.venv/bin/python",
        "python.terminal.activateEnvironment": true,
        "jupyter.notebookFileRoot": "${workspaceFolder}"
      },
      "extensions": ["ms-python.python", "ms-toolsai.jupyter"]
    }
  },
  "remoteUser": "vscode"
}
